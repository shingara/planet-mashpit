<h1>Edit user : <%= @user.login %></h1>

<%= error_messages_for @user %>

<%= form_for(@user, :action => resource(:admin, @user)) do %>
  <%= text_field :login, :label => 'login' %>
  <%= submit 'Create' %>
<% end =%>
